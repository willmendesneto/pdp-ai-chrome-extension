import { bootstrapApplication } from '@angular/platform-browser';
import { OptionsComponent } from './app/options.component';

bootstrapApplication(OptionsComponent).catch((err) => console.error(err));
